   
    <div class="breadcumb-wrapper">
        <div class="container">
            <ul class="breadcumb-menu">
                <li><a href="{{ path('app_home') }}">Home</a></li>
                {% if titre is not null %}
                    <li>
                        <a href="{{ path('app_category', {'slug': slug}) }}">
                            {{ titre }}
                        </a>
                    </li>
                {% endif %}
                <li>{{ title }}</li>
            </ul>
        </div>
    </div>